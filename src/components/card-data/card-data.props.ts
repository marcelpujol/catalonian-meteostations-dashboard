export interface CardDataProps {
    code: string;
    icon: string;
    label: string;
    unit: string;
    value: number;
    max: number;    //max supported value
    min: number;    //min supported value
}